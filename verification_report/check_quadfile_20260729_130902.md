# Verification Report — Quadfile

*Generated 2026-07-29 13:09 · read-only check · every number is explained, not judged.*

> This report explains, in plain English, **what the tool found**. It does not decide what is good or bad — each number comes with what it counts, so your team can read it without needing us.

---

### File parses

.\.platform_runs\quads\TICTACTOE.yaml

*What this means:* Whether the analyzer's output file is readable.

### Analyzer version

produced by analyzer 3.4.2

*What this means:* Which analyzer produced this file — so a result can always be traced to the exact reader that made it.

### What this run read

python 1/1 · workflows 0 · terraform 0 · java 0 · config-SQL 0 · zips NOT opened 0

*What this means:* How many files of each kind the analyzer read — a zero here means the files were not in the upload, stated outright instead of hiding behind ordinary-looking output. Zips are never opened; if any are present they are listed so nothing silently rides inside one.

### Entities

| Count | What it is | What it means |
|---:|---|---|
| **11** | Functions | individual code functions |
| **1** | Modules | code files / modules |

*What this means:* The individual things the analyzer recorded in this file.

### Facts

23 total across 2 relationship kinds

*What this means:* The relationships the analyzer recorded (X calls Y, X writes table Z, …).

### Journeys walked

0 journeys + 0 behavior families — ZERO journeys: the app may be all small behaviors, or the graph has no walkable chains (send this report)

*What this means:* How many end-to-end flows the analyzer traced in this file.

### Repo-declared answers

0 bindings harvested from the repo

*What this means:* Placeholder values the tool found already written down in the repo's own config.

### Web calls (outbound APIs)

0 distinct outside address(es) the code calls — none found in the code

*What this means:* Outside services the code calls over the web (payment, data-provider, partner APIs), found by the address in the call itself.

### Secrets read (names only)

0 secret name(s) the code fetches — the names only; secret VALUES are never read or stored

*What this means:* Which stored credentials the code fetches, by NAME only — the secret's value is never read, never stored, never shown.

### Parameters read

0 parameter-store name(s) the code fetches

*What this means:* Configuration values the code fetches from the parameter store, by name.

### Notifications published

0 notification topic(s) the code publishes to

*What this means:* Notification topics the code publishes messages to.

### Database tables

0 distinct table(s) — 0 read, 0 written — no SQL found in code or config

*What this means:* Every database table the app reads or writes — including queries kept in config files rather than in the code.

### Bucket names readable

0 of 0 storage fact(s) carry a real, readable object name (0 distinct bucket(s))

*What this means:* Whether storage facts show real bucket and file names, or still show placeholders/raw code text whose values were never supplied.

### Lambda code links

no lambdas declared in this upload

*What this means:* Whether each declared lambda is connected to the code it runs — flows can only show what a step does if this link exists. Unlinked lambdas are listed as questions, never guessed.

### Environment

single-environment upload — no competing settings files

*What this means:* Which environment's settings ground this map (test, devl, …) — one environment only; the others present are deliberately not mixed in.

### Lambda names confirmed

no terraform-declared lambda names in this upload

*What this means:* Whether a person has reviewed the lambda names the deploy files declare — some estates write only half the name there, so tests must not trust them unreviewed.

### Placeholders

0 unresolved facts; 0 distinct ${} tokens — nothing needs the name checkpoint

*What this means:* Names still unresolved — their real value comes from configuration, not the code.

### Blocked or complete

COMPLETE — every name is answered or decided; journeys were computed

*What this means:* Whether this run finished, or stopped because some names still need a human's answer (a stopped run carries no journeys, on purpose).

### Human-supplied answers

0 name(s) supplied by a person, producing 0 human-stamped fact(s) — each is marked so it can never be mistaken for something the code said

*What this means:* Names a person typed in. Every fact built from one is marked as human-supplied, so it can never be mistaken for something the code said.

### Skip / runtime decisions

none — no name was skipped

*What this means:* Names a person deliberately left blank (with their reason). The map keeps a visible hole there instead of a guess.

### Worksheet warnings

0 operator-sheet issue(s) — the answer sheets are consistent

*What this means:* Inconsistencies in the answer sheets — an answer for a name that doesn't exist, or a name both answered and skipped.

### Needs wiring

1 journey-shaped fragment(s) that no front door reaches — held for review, NOT promoted to journeys: Module:.

*What this means:* Pieces that look like full flows but that nothing reachable starts — held out for review instead of being counted as journeys.

### App id

TICTACTOE

*What this means:* The name this app is filed under.

### Ready to ingest

READY TO INGEST — with known, visible holes: 1 needs-wiring fragment(s)

*What this means:* The decision line: READY means the run is complete and this file can be loaded now; WAIT means names still need a human's answer (the file carries no journeys yet) or the file is unhealthy.

---

*Terms: a **journey** is one end-to-end flow; a **behavior group** is a family of small behaviors; a **placeholder** (`${…}`) is a name whose real value comes from configuration. This is a read-only report — no data was changed. The raw numbers are also saved as JSON next to this file.*
