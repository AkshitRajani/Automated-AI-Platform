# =============================================================================
# AUTO-GENERATED by constructor/build_compose.py — do not hand-edit.
# Regenerate with: python3 -m constructor up <profile_dir> --emit-only
# =============================================================================
services:
  localstack:
    image: localstack/localstack:4
    ports:
    - 4566:4566
    environment:
      LOCALSTACK_PERSISTENCE: '0'
      SERVICES: lambda,s3,dynamodb,stepfunctions,glue,iam,sts
      LAMBDA_DOCKER_NETWORK: core_sandbox-net
      DEBUG: '0'
    volumes:
    - /var/run/docker.sock:/var/run/docker.sock
    networks:
    - sandbox-net
    depends_on:
    - microcks
    - postgres
  microcks:
    image: quay.io/microcks/microcks-uber:1.10.0
    ports:
    - 8080:8080
    networks:
    - sandbox-net
  postgres:
    image: postgres:16
    ports:
    - 5433:5432
    environment:
      POSTGRES_DB: aap_sandbox
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
    - ./profiles/default/attachments/postgres/ddl/generated:/docker-entrypoint-initdb.d:ro
    networks:
    - sandbox-net
  runner:
    image: python:3.11-slim
    environment:
      AWS_ENDPOINT_URL: http://localstack:4566
      DBM_BASE_URL: http://microcks:8080/dbm
      CAL_BASE_URL: http://microcks:8080/cal
      POSTGRES_DSN: postgresql://postgres:postgres@postgres:5432/aap_sandbox
      AWS_DEFAULT_REGION: us-east-1
      AWS_ACCESS_KEY_ID: test
      AWS_SECRET_ACCESS_KEY: test
      SANDBOX_SEED: '42'
    volumes:
    - ./:/workspace:rw
    working_dir: /workspace
    networks:
    - sandbox-net
    depends_on:
    - localstack
    - microcks
    - postgres
    command: tail -f /dev/null
networks:
  sandbox-net:
    driver: bridge
