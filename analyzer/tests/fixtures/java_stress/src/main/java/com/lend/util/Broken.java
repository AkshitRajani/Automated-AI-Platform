package com.lend.util;

// Deliberately unparseable — the analyzer must skip this file gracefully,
// never crash and never emit half-facts from it.
public class Broken {{{
    void oops(( {
        this is not java at all %%%%
